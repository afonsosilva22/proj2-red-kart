# Backend API URL (Spring Boot default port)
VITE_API_URL=http://localhost:8080

# Customer ID whose rentals are shown in the web app (no auth yet)
# João Silva (id 4) has rentals in the sample database
VITE_CUSTOMER_ID=4
